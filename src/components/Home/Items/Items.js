import React from 'react';
import Item from '../Item/Item';

const Items = () => {
    return (
        <div>
            <h2>This is item section</h2>
            {/* {
                Items.map(item => <Item
                key ={item.id}
                item={item}
                ></Item>)
            } */}
        </div>
    );
};

export default Items;