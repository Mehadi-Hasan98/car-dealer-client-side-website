import React from 'react';

const Item = ({item}) => {
    // const {id, name, price, img, description} = item;
    return (
        <div>
            {/* <img src={img} alt="" />
            <p>{name}</p>
            <p>{price}</p> */}
        </div>
    );
};

export default Item;